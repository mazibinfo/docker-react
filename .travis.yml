language: python

python:
  - "3.8"

# Install dependencies (if any)
install:
  - pip install -r requirements.txt

# Run the tests and echo "Hello, World!"
script:
  - echo "Hello, World!"
  - pytest
